# blogs-docker-crawler-api-supervisor


## Steps


1 ) Move to directory
```
cd blogs-docker-crawler-supervisor
```

2 ) Set/Replace all params in env/.env   
3 ) Push your docker image
```
bash ./bin/push_image.sh  supervisor
```